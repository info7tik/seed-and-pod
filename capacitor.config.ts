import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seedandpod.app',
  appName: 'seed-and-pod',
  webDir: 'dist/seed-and-pod/browser'
};

export default config;
