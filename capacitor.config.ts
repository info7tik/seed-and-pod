import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.seedandpod.app',
  appName: 'seed-and-pod-mobile',
  webDir: 'dist/seed-and-pod-mobile/browser'
};

export default config;
